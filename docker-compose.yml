services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: ceinfua
      POSTGRES_PASSWORD: ceinfua
      POSTGRES_DB: ceinfua
    network_mode: host
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
